- **Fix: Desktop sidecar bundles dependencies with mismatched dist/module names**: PyPI packages whose distribution name differs from their importable module name (e.g. `jasketch-mcp-server` ships top-level module `jasketch_mcp`) are now correctly bundled into the frozen desktop sidecar. `.jac` source files shipped inside Python distributions are bundled alongside the `.py` files.
