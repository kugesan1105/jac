- **Fix: Desktop apps installed at read-only system paths no longer crash on startup**: Frozen sidecars installed under system paths like `/usr/lib/<app>/` (e.g. via `.deb` or `.rpm`) now write their SQLite identity store to the user's data directory instead of crashing with `PermissionError` and never reaching the webview.
