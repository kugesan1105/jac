- **Fix: client runtime `dict`/`list` annotations now declare element types**: 38 bare `dict`/`list` parameters and returns in `client_runtime.cl.jac` and its impl now spell out their element types — `dict[str, any]` / `list[any]` for genuinely heterogeneous JSX prop bags, RPC payloads, and hook return tuples; `dict[str, str]` for CSS style maps; `list[str]` where the site iterates strings. Clears all 38 W1036 warnings in the file with no runtime change.
