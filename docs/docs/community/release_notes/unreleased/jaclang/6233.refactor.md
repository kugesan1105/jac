- **Refactor: declare `EsastGenPass` instance attributes**: The ECMAScript codegen pass initialized 19+ fields inside `before_pass` without declaring them, leaving the type checker blind and producing ~240 false-positive diagnostics on every check of the pass. Declaring the fields drops `jac check` noise on `esast_gen_pass.impl.jac` from 361 to 200 diagnostics (−45%) with no behavior change.
