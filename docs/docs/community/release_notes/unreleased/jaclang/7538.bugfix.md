- **Fix: make `type_utils` and the version-condition helpers type-clean (clears the last full-repo `jac check` baseline error)**: `expr_primitive_name` annotated `prog` as `object`, so `prog.get_type_evaluator()` reported E1030; it is now `JacProgram` via a type-only import. The version-condition helpers in `helpers.jac` annotated their parameters with a forward-ref `'uni.Expr'` (uni is imported only inside the bodies), which the checker read as `Literal["uni.Expr"]` and rejected at the call site; they now use a module-level type-only `UniNode` import. Also handles `str | bytes` docstrings, guards `None` `symbol_table`/`type_params`/`type_args`, and returns the enum member's int `.value` in `completion_kind_from_sym`.
