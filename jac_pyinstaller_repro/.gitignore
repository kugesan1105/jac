# PyInstaller build artifacts
build/
dist/
*.spec

# Jac compile caches
**/.jac/
__pycache__/

# Workaround-generated files (created by apply_workaround.sh)
# Keep them ignored so the broken-by-default state stays broken
myapp/**/__init__.py
